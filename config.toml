# The URL the site will be built for
base_url = "https://projects.cameron.rs"
title="cameron - software engineer"

description = "This is where I host some of my projects"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Put all your custom variables here
menu_links = [
    { url = "$BASE_URL/", name = "Projects" },
    { url = "https://blog.cameron.rs", name = "Blog" },
    { url = "https://docs.cameron.rs", name = "Documentation" },
]

projects = [
    { name = "sudoku-solver", description = "Sudoku Solver using bitmasks and bit-manipulation with Rust 🦀 and egui 🎨", url = "$BASE_URL/sudoku-solver" },
]